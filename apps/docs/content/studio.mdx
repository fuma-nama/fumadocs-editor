---
title: Studio
icon: PenLine
description: Edit a folder of MDX files without writing a host app
---

Studio is the editor as a command. Run it in your Fumadocs app:

```bash
npx @fumadocs-editor/studio
```

It opens a browser with your documents and a file list:

- edits save to disk, and changes on disk merge in ([Sync](/sync))
- the [file tree](/file-tree) creates, deletes and reorders pages
- `?collab` in the URL turns on [collaboration](/collab) for that tab

<Callout type="info">Studio needs Node 24 or later.</Callout>

## Keyboard

| Keys  | Does                                                 |
| ----- | ---------------------------------------------------- |
| `⌘K`  | search files and actions (theme, collab, MDX source) |
| `⌘S`  | save now                                             |
| `Esc` | close the file list, when focused                    |

The file list has its own [shortcuts](/file-tree#keyboard).

## Flags

| Flag              | Default                                 |
| ----------------- | --------------------------------------- |
| `--root <dir>`    | `content/docs`, `content`, then the cwd |
| `--config <file>` | `fumadocs-studio.config.ts`             |
| `--port <n>`      | `5180`                                  |
| `--host`          | listen on all addresses                 |
| `--no-open`       | do not open the browser                 |

## Configuration

Create a `fumadocs-studio.config.ts` where you run Studio. Flags override it.

<include meta='title="fumadocs-studio.config.ts"'>../examples/studio-config.ts</include>

<auto-type-table path="../../../packages/studio/src/config.ts" name="StudioConfig" />

`components`, `syntax`, `theme` and `media` work like the
[`MdxEditor` props](/editor). Your own components follow the
[authoring guide](/components), and their styles load through `styles`.

### Server options

The config also runs in the browser, so anything Node-only (a JWT library,
a Vite plugin) goes in a separate file referenced by `server`:

<include meta='title="studio.server.ts"'>../examples/studio-server.ts</include>

<auto-type-table path="../../../packages/studio/src/config.ts" name="StudioServerConfig" />

## Auth

Without `authenticate`, anyone who can reach the port can edit. With it,
the credential comes from `?token=` in the URL by default; override it with
`auth`. See [Auth](/auth) for writing the hook.
