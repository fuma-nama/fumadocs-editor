---
title: Read-only
icon: Eye
description: Show a document without letting it change
---

`editable={false}` shows the document read-only:

- no typing, and no mutating chrome (slash menu, bubble and block menus,
  joystick)
- selection and copy still work
- the raw MDX tab is read-only too

The prop is live: flipping it does not remount the editor. It is a plain
TipTap passthrough and has no opinion about _why_: a viewer role, a
published snapshot, a document checked out by someone else.

## Wiring it to auth

The [auth layer](/auth) enforces scopes on the server and reports them: the
file's read reply carries `writable`, and `sync.onOpen` hands it to you.
Connecting that to `editable` is your code:

<include meta='title="scoped-editor.tsx"'>../examples/read-only.tsx</include>

`onOpen` fires the same way with `collab` on.

<Callout type="info" title="Not a security boundary">
  A read-only user who tampers with the client and types anyway changes nothing: the server refuses
  the writes. Under [collab](/collab), their updates are dropped while they keep receiving everyone
  else's. `editable` is UI, not enforcement.
</Callout>
